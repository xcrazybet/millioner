{
  "name": "millioner-gaming-platform",
  "version": "1.0.0",
  "description": "Premium gaming platform with Firebase",
  "main": "index.html",
  "scripts": {
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:all": "firebase deploy",
    "setup:admin": "node setup-admin.js",
    "test:permissions": "open test-permissions.html"
  },
  {
  "scripts": {
    "build": "your-build-command",
    "deploy": "firebase deploy --only hosting"
  },
}
  "dependencies": {
    "firebase": "^9.22.0",
    "firebase-admin": "^11.11.0"
  },
  "devDependencies": {
    "firebase-tools": "^12.9.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  {
  "hosting": {
    "public": "build", // or "dist" or "public" depending on your framework
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [...]
  }
}
